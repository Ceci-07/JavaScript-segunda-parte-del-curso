//Este método es muy similar al metodo for, lo que hace en una sintaxis mucho más chica.

// array.forEach((item) => {
//   //código a ejecutar
// });

let list = ["eat", "sleep", "code", "repeat"]

list.forEach((item) => {
  console.log(item);
})
